export interface IBlock {
    position: number,
    dcoumentPath: string,
    classificationType: string,
    classificationScore: string,
    columnName: string,
    columnValue: string,
    vertices: Object,
    autoSuggestOptions: string[]
}